# 香港交易所财报产品收入提取 — 优化提示词

## 任务

优化 `HKCO_FN_PRODUCT` 提取流水线，从 1061 份港交所 PDF 公告中提取 `product_name`、`mbrevenue`、`mbcost`、`gross_profit`、`start_date`、`end_date`。

## 文件结构

| 文件 | 作用 | 修改频率 |
|------|------|----------|
| `run_backtest.py` | 回测入口：跑提取 → 对 GT → 出报告 | **禁止修改** |
| `custom/service/EAPS_HKCO_FN_PRODUCT.py` | **选表模块**：PDF 解析 → 章节拆分 → 候选表打分 → 选最优表 | 中 |
| `custom/service/EAPS_HKCO_FN_PRODUCT_get_res.py` | **提取模块**：分类表类型 → 拆 header/body → 找产品名 → 找收入行 → 提取金额 | **高** |
| `custom/service/EAPS_HKCO_FN_PRODUCT_format_data.py` | **格式化输出**：过滤噪声标签 → 标准化日期/币种/单位 | 中 |
| `debug_pipeline.py` | 单文档全链路调试 | 只读 |
| `tasks/HKCO_FN_PRODUCT/ground_truth.json` | GT 标注（infocode → [{PRODUCTNAME, MBREVENUE, ...}]） | 只读 |
| `tasks/HKCO_FN_PRODUCT/last_data.json` | 上期 LP 数据（用于产品名匹配和选表） | 只读 |
| `all_selected.json` | 离线缓存的选表结果（`debug_pipeline.py` 用） | 只读 |

## 流水线全链路

```
PDF → get_lines() → get_target_tables() → get_target_table() → get_res() → format_data()
     解析成lines    章节拆分+表合并      候选表打分+选最优    分类+提取产品金额   过滤+标准化
```

**选表 > 分类 > 提取**。表选错了后面全错。改代码前先确认选的表对不对。

## 表类型分类（`classify_table` in get_res.py）

| 类型 | 含义 | 提取函数 |
|------|------|----------|
| 收入_行产品 | 产品名在 col0，收入表 | `extract_type1` |
| 收入_列产品 | 产品名在列头，收入表 | `extract_type2` |
| 分部_行产品 | 分部表，产品在 col0 | `extract_type1` |
| 分部_列产品 | 分部表，产品在列头 | `extract_type2` |
| 损益 | P&L 表（可能有嵌入式产品行） | `extract_type3` |
| 损益_单产品 | 单产品公司 P&L | `extract_type4` |
| 百度 | 百度多期间块表 | `extract_type5` |

分类决策链：`is_row_product()`（LP 产品名匹配 + `_table_structure_signal`）→ 标题关键词 → `_has_cost_section` 追加 `_含成本段` 后缀。

## 核心调试方法

```bash
# 最重要的调试工具 — 单文档全链路
python debug_pipeline.py AN202603251820734009

# 输出：完整表格 → header/body 拆分 → LP 匹配追踪 → 分类结果 → 提取结果 → GT 对比
# 每个字段都打印出来，一眼看出问题在哪一步

# 重跑完整 PDF 选表流程（检查是否选错表）
python debug_pipeline.py AN202603251820734009 --pdf

# 批量回测
python run_backtest.py --task HKCO_FN_PRODUCT

# 查看最新结果
ls -t batch_runs/HKCO_FN_PRODUCT/ | head -1

# 分析问题分布
python3 -c "
import json
with open('batch_runs/HKCO_FN_PRODUCT/<timestamp>/metrics/per_doc.json') as f:
    data = json.load(f)
# 按 error pattern 聚类分析
"
```

## 系统性分析方法（不要一个一个 doc 调）

### 1. 先看宏观分布
```python
# 问题 doc 按错误类型分类
pure_value = [r for r in data if r['missing']==0 and r['extra']==0 and r['value_diff']>0]
pure_extra = [r for r in data if r['extra']>0 and r['missing']==0 and r['value_diff']==0]
pure_missing = [r for r in data if r['missing']>0 and r['extra']==0 and r['value_diff']==0]
mixed = [r for r in data if r not in pure_value+pure_extra+pure_missing]

# field_accuracy_detail 看哪个字段错最多
fd = r['stats']['field_accuracy_detail']
# MBREVENUE / MBCOST / GROSS_PROFIT / UNIT / CURRENCY 各自的 ok/mismatch
```

### 2. 找错误模式（不是个案）
```python
from collections import Counter
# extra 物品名聚类：是时间标签？地理标签？小计标签？
extra_names = Counter()
for r in data:
    for item in r.get('extra_items', []):
        name = item.get('predicted', {}).get('PRODUCTNAME', '')
        # 归类...
# missing 根因：产品名在不在选中的表里？
for r in data:
    gt_products = [item['expected']['PRODUCTNAME'] for item in r.get('missing_items', [])]
    flat = ' '.join(str(c) for row in tbl for c in row)
    found = sum(1 for p in gt_products if p in flat)
    # found==0 → 选错表; found==len → 提取逻辑问题
```

### 3. 验证候选表是否更好
```python
# 检查 candidate_N_target_item.json 里有没有 GT 产品名
for fname in os.listdir(debug_dir):
    if fname.startswith(code + '_candidate_'):
        # 读候选表，检查 GT 产品名命中数
```

## 关键代码位置和常见问题

### get_res.py — 提取层（改得最多）

| 函数 | 行号 | 作用 | 常见问题 |
|------|------|------|----------|
| `classify_table` | ~168 | 决定表类型 | 标题过短(≤2字符)误拒绝 → 回退检查LP匹配 |
| `_has_cost_section` | ~275 | 检测成本段 | `_COST_SECTION_RE` 不匹配裸"銷售成本" |
| `_detect_subcolumn_metrics` | ~1000 | 检测金額/百分比列 | header行未左补齐导致列错位 |
| `_is_noise_label` | ~350 | 噪声标签过滤 | 需持续补充IFRS15时间/地理/小计模式 |
| `_PL_ROW_LABEL_RE` | ~325 | P&L行标签匹配 | 同上 |
| `extract_type2` header扫描 | ~1500 | 列产品名检测 | span行(≥60%同文)跳过; "及"续名拼接 |
| `extract_type2` Pass 1-4 | ~1580 | 收入行选择 | 所有pass统一排除"利息"; Pass2不抢"其他"子项; Pass4优先空col0合计行 |
| `extract_type1` 产品过滤 | ~1230 | 行产品名过滤 | `_SECTION_TITLE_RE` 过滤时间/地理标签 |
| `_detect_unit` | ~109 | 单位检测 | 大量级启发式(>1e8→元) |
| `_inject_cost_and_profit_from_full_table` | ~780 | GP/MBCOST注入 | 从截断前全表提取 |

### EAPS_HKCO_FN_PRODUCT.py — 选表层

| 函数 | 行号 | 作用 | 常见问题 |
|------|------|------|----------|
| `_is_inter_table_glue` | ~237 | 表间是否可合并 | 期间横幅含"分部/业绩"关键词不应合并 |
| `_pick_best` | ~2268 | 候选表排序 | `valid_hits=min(hits,amt)` 为主键，排除有LP名无金额的叙事文本 |
| `_pick_table_by_last_period_names` | ~2534 | LP名匹配选表 | hits相同时交给_pick_best裁决 |
| `_validate_revenue_table` | ~2771 | 校验不是纯P&L | >70%成本行+<20%产品命中→拒绝 |
| `_select_by_history` | ~2831 | 有LP数据时的主选表流程 | 验证失败后retry一次 |

### format_data.py — 格式化层

| 位置 | 作用 |
|------|------|
| 噪声标签过滤 (~120) | `re.match` 排除P&L/BS标签 |
| IFRS15时间标签过滤 (~147) | 排除时间点/段变体 |
| 地理标签过滤 (~152) | 排除"中國內地"等 |
| 小计净额过滤 (~160) | 排除"xxx總額/淨額" |

## 已验证有效的修复策略

### 优先级 1：系统性过滤器（影响面最大）

**找规律 → 加模式 → 三处同步**（get_res 的 `_SECTION_TITLE_RE`/`_is_noise_label`/`_PL_ROW_LABEL_RE` + format_data 的噪声过滤 + extract_type2 的 header 扫描）。

已加模式：
- IFRS15 时间标签：`於某一時間點|隨時間|於一段時間內|...`
- 地理标签：`中國內地|香港|海外|馬來西亞|...`
- 小计净额：`*總額$|*淨額$|*總計$`
- 非核心收入：`股息收入|利息收入|其他收入|...`

### 优先级 2：收入行选择（影响纯值错）

- Pass 2 不 break on first match → 收集候选，优先非"其他"标签、更短标签
- 所有 Pass（1/2/3/3.5）统一加 `利息` 排除词
- Pass 4 不再取第一个有数字的行 → 优先空 col0 合计行，其次最多有效列

### 优先级 3：表选择（影响 missing）

- `_pick_best` 主键改为 `min(hits, amt)`：LP 名命中但无对应金额的叙事文本被降权
- `_is_inter_table_glue`：含分部/业绩关键词的期间横幅不合并表
- `classify_table`：标题过短时回退检查表内 LP 匹配

### 优先级 4：字段级修复

- `_detect_subcolumn_metrics` 左补齐 → 金額/百分比列不错位
- `_COST_SECTION_RE` 允许 `$` 结尾 → 裸"銷售成本"匹配
- `_detect_unit` 大量级启发式 → UNIT 错 -58%

## 剩余问题分布（748 docs，基线 795）

| 问题 | 数量 | 根因 |
|------|------|------|
| missing (产品在表内但未提取) | ~680 行 | 各 doc 根因不同：收入行选错、列头漏扫、名字变体不匹配 |
| missing (产品不在选中表) | ~200 行 | 选表逻辑仍有个案问题 |
| 纯值错 | ~170 docs | GROSS_PROFIT 缺失(50%)、REVENUE 选错(40%) |
| extra | ~1100 行 | 剩余非产品行泄漏 |
| mixed | ~360 docs | 多因素组合 |

## 关键经验

1. **先做宏观分析，找规律再改代码**。不要 debug 一个 doc 改一处代码——那是贴狗皮膏药。用 `per_doc.json` 统计错误模式，找到影响最大的一个规律，一次性修。

2. **过滤器要三处同步**。`_SECTION_TITLE_RE`（extract_type1）、`_PL_ROW_LABEL_RE`（通用）、`format_data` 噪声过滤 —— 加新模式时三处都要更新，否则从不同路径进来的数据可能漏网。

3. **选表 > 分类 > 提取**。表选错后面全错。`_pick_best` 的排序键决定一切。`valid_hits = min(hits, amt)` 这个改动虽小，修了两个最坏 case（30+ extra → 0）。

4. **不要兜底/fallback 逻辑**。在分类/选表层面修根因，不要在提取层面加"如果不行就试试别的"。

5. **LP 数据是最可靠的信号**。`is_row_product` 用 LP 名匹配判断行/列产品；`_last_period_amount_evidence` 用 LP 名+金额判断表质量。


不能改 run_backtest.py
